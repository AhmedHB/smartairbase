package se.smartairbase.mcpserver.domain.game.enums;

public enum AircraftStatus {
    READY,
    ON_MISSION,
    AWAITING_DICE_ROLL,
    AWAITING_LANDING,
    PARKED,
    WAITING_MAINTENANCE,
    IN_MAINTENANCE,
    HOLDING,
    CRASHED,
    DESTROYED
}
