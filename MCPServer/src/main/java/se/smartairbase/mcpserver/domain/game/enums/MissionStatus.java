package se.smartairbase.mcpserver.domain.game.enums;

/**
 * Lifecycle states for one mission inside a live game.
 */
public enum MissionStatus {
    AVAILABLE,
    ASSIGNED,
    COMPLETED,
    FAILED
}
